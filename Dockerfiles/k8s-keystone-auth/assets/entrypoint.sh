#!/usr/bin/bash

set -xe

# Sample syntax to launch the webhook.
# /usr/local/bin/k8s-keystone-auth \
#   --tls-cert-file ${TLS_CERT_FILE} \
#   --tls-private-key-file ${TLS_CERT_PRIVATE_KEY} \
#   --keystone-policy-file ${K8S_KEYSTONE_POLICY_FILE} \
#   --keystone-url ${K8S_KEYSTONE_URL}
